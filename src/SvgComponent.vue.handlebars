<script setup lang="ts">
{{#if defineProp}}
defineProps<{
  solid: {
    type: boolean,
    required: true,
  }
}>();
{{/if}}
</script>

<template>
{{#each icons}}
  <template
  {{~#if ../defineProp}}
  v-if="
      {{~#if solid~}}
      solid
      {{~else~}}
      !solid
      {{~/if~}}
      "
  {{~/if~}}
  >
    {{{this.svg}~}}
  </template>
{{/each}}
</template>

<style scoped>

</style>