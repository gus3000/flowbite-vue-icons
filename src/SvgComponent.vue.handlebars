<script setup lang="ts">
{{#if defineProp}}
defineProps<{
  solid: {
    type: boolean,
    required: true,
  }
}>();
{{/if}}
</script>

<template>
{{#each icons}}
  {{~#if ../defineProp~}}
  <template v-if="
      {{~#if solid~}}
      solid
      {{~else~}}
      !solid
      {{~/if~}}
      ">
  {{~/if~}}
    {{{this.svg}~}}
  {{~#if ../defineProp~}}
  </template>
  {{~/if~}}
{{/each}}
</template>

<style scoped>

</style>